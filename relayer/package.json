{
  "name": "zk-crosschain-payment-channel-relayer",
  "version": "0.1.0",
  "private": true,
  "description": "Watches Chain A, generates ZK proofs of its state, submits them to Chain B's LightClientVerifier. Milestone 3 — see PLAN.md.",
  "scripts": {
    "deploy": "node src/deploy.js",
    "relay": "node src/index.js",
    "e2e": "node src/e2e_demo.js"
  },
  "dependencies": {
    "ethers": "^6.13.0",
    "snarkjs": "^0.7.5"
  }
}
