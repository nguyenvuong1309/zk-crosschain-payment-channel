{
  "name": "zk-crosschain-payment-channel-circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Circom circuits for the ZK cross-chain payment channel demo (Milestone 2: channel_state.circom)",
  "scripts": {
    "build:channel_state": "circom circuits/channel_state.circom --r1cs --wasm --sym -l node_modules -o build",
    "gen:input": "node input_gen/generate_channel_state_input.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.5"
  }
}
